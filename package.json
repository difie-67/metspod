{
  "name": "escrow-bot-ton-only",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:contract": "rm -rf contracts/output && tact --config ./tact.config.json && mkdir -p contracts/wrappers && cp \"$(find contracts/output -type f -name '*Escrow*.ts' | head -1)\" contracts/wrappers/Escrow.ts",
    "typecheck": "tsc --noEmit",
    "dev": "node --import tsx src/index.ts",
    "build": "echo \"OBRA BUILD PHASE: 2026-09-24_15-18_dealux8_stylepass\" && test -f tact.config.json && tact --config ./tact.config.json && tsc -p tsconfig.json --outDir dist && rm -rf dist/miniapp && cp -R miniapp dist/miniapp",
    "start": "node dist/src/index.js",
    "test": "npm run build:contract && node --import tsx tests/escrow.spec.ts",
    "admin": "node --import tsx scripts/admin.ts",
    "wallet:new": "node --import tsx scripts/generate-wallet.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tact-lang/compiler": "^1.6.13",
    "@ton/sandbox": "^0.44.0",
    "@types/node": "^26.6.1",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@ton/core": "^0.63.0",
    "@ton/crypto": "^3.3.0",
    "@ton/ton": "^16.3.0",
    "dotenv": "^18.0.0",
    "grammy": "^1.46.0"
  }
}
